# Core deps — pinned to the versions this project is developed and tested against.
# Bump deliberately; a floating range here is how a working clone silently stops working.
fastapi==0.138.1
uvicorn[standard]==0.49.0
websockets==16.0
PyYAML==6.0.3
rich==15.0.0
python-dotenv==1.2.2
requests==2.34.2
jsonschema==4.26.0
python-dateutil==2.9.0.post0
dateparser==1.4.1
prompt_toolkit==3.0.52

# System / hardware telemetry
psutil==7.2.2

# Optional GPU info — import is guarded, safe to omit on non-NVIDIA machines
nvidia-ml-py==13.610.43

# Voice recognition — optional, guarded at import
faster-whisper==1.2.1
sounddevice==0.5.5
numpy==2.2.6

# OS sandbox — optional, guarded at import
PyAutoGUI==0.9.54
